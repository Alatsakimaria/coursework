Ο κωδικας οριζει δυο μεθοδους με το ιδιο ονομα αλλα με διαφορετικους τυπους παραμετρων. Η μια παιρνει ενα Predicate<Long> p
και η αλλη ενα  LongPredicate p. Αρα οταν καλεσουμε την main ο μεταγλωττιστης δεν μπορει να προσδιορισει ποια απο τις δυο
να καλεσει. Για να το λυσουμε αυτο μπορουμε να αλλαξουμε το method((Predicate<Long>) (x)->true ή method(LongPredicate :: test)
για να επιτρεψει τον μεταγλωττιστη να καθορισει ποια απο τις 2 μεθοδους να καλεσει.